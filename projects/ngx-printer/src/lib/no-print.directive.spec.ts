import { NoPrintDirective } from './no-print.directive';

describe('NgxNoPrintDirective', () => {
  it('should create an instance', () => {
    const directive = new NoPrintDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
