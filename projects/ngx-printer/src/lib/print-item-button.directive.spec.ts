import { PrintItemButtonDirective } from './print-item-button.directive';

describe('PrintItemButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new PrintItemButtonDirective();
    expect(directive).toBeTruthy();
  });
});
