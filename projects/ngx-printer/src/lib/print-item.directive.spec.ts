import { PrintItemDirective } from './print-item.directive';

describe('PrintItemDirective', () => {
  it('should create an instance', () => {
    const directive = new PrintItemDirective();
    expect(directive).toBeTruthy();
  });
});
