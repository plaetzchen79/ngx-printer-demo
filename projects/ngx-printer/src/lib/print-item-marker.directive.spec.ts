import { PrintItemMarkerDirective } from './print-item-marker.directive';

describe('PrintItemMarkerDirective', () => {
  it('should create an instance', () => {
    const directive = new PrintItemMarkerDirective();
    expect(directive).toBeTruthy();
  });
});
